# T4CT — two-photon Ca2+ imaging: denoise + motion-correct + segment + traces
#
# torch, numpy, scipy, scikit-image, scikit-learn, matplotlib, pandas, tqdm and
# tifffile are ALREADY on Colab. Do NOT pin torch — Colab ships its CUDA build.
#
# suite2p runs headless on Colab via its Python API (run_s2p) — no GUI needed.
# It pulls a numpy/numba combo that differs from Colab's default (numba needs
# numpy <= 2.2), so a ONE-TIME runtime restart is required after the first
# install. The notebook's install cell does this automatically (it restarts the
# runtime; just Run all again and it skips ahead).

# --- Core pipeline: motion correction + ROI detection + deconvolution + traces ---
suite2p>=1.0,<2

# --- IO (recent tifffile handles the big multi-page recording well) ---
tifffile>=2023.7.10

# --- Alternatives / advanced milestone (install on demand; see README) ---
# caiman               # CNMF pipeline — conda-based, harder to install on Colab
# deepcad              # DeepCAD-RT self-supervised denoising (PyTorch, uses the T4)
# deepinterpolation    # DeepInterpolation denoising (TensorFlow)
